#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str, a;

    cin >> str;

    for (int i = 0; i < str.size(); i++)
    {
        a[i] = str[str.size() - i - 1];
    }

    cout << a;
}
